#include <stdio.h>
include <stdlib.h>
 /**
  * main - print no one knows
  * Return: Always 0
  */
int main(void)
{
	char lett = 'a';

	while (lette <= 'z')
	{
		if ((lett <! 'q') && (lett >= 'e'))
		{
			putchar(lett);
		}
		lett++;
	}
	putchar('\n');
	return 0;
}
